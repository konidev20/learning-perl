#!/usr/bin/perl

# types of variables in perl
# 1. scalars

# scalars start with a $ sign
# $variable and $Variable are different (variable names are case sensitive)
# variable names cannot start with a number and cannot contain special characters like @, #, - etc.
$hostname = "127.0.0.1";
$port = 80;

print("hostname: ". $hostname ." port: ". $port ."\n")

# you can use variables before declaring it

# 2. lists 
# 3. hashes